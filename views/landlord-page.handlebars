<form class="search-button">
   <input type="text" placeholder="Property Address" name="search">
   <button class="propSearchBtn" type="submit">Search</button>
</form>

<section>
   <ol>
      {{#each landlords}}
      <li>
         {{> landlord-info}}
         {{!-- {{MOST RECENT COMMENT}} --}}
         {{!-- {{> comment}} --}}
      </li>
      {{/each}}
   </ol>
</section>